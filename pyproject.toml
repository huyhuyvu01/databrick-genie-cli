[project]
name = "genie-cli"
version = "0.1.0"
description = "Databricks Genie CLI"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "databricks-sdk>=0.1.0",
    "rich>=13.0.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
